meta {
  name: Unsave Job
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/save-jobs/{{jobId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  Removes a job from the authenticated user's saved jobs.
}
